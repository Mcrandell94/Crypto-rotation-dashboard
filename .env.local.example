# Copy this file to .env.local for local testing and fill in your real key.
# NEVER commit .env.local to git - it's already excluded in .gitignore below.
# In production (Vercel), this is set instead under:
#   Project Settings > Environment Variables > CMC_API_KEY

CMC_API_KEY=your-coinmarketcap-api-key-here
